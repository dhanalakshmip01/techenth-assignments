1. Run two containers hr1 and emp1 from busybox. busybox is a image which contain ping utility.
2. Login to hr1 container and check the ip address using ip address show cmd
3. check ->  is hr1 is able to  ping google.com or not.
4. login to emp1 container and ping hr1 ip address from emp1 container.
5. login to hr1 container and ping emp1 ip adress.
